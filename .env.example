# Database Connection
DATABASE_URL=postgresql://postgres:password@localhost/envelope_encryption

# Database Encryption (encryption at rest)
# Note: Database encryption handles KEKs at rest
# KEKs are stored as plaintext in the database (encrypted at rest by database encryption)
